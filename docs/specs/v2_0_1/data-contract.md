# OCPP 2.0.1 — Data Contract

Formats this area owns: wire layouts, file schemas, field widths and order,
value ranges, encodings.

Normative. A format is the hardest kind of contract to change after release —
every entry here is settled at gate 1, with the exact bytes or the exact schema,
not a description of them.

---

## Layout

*(Empty. One table per message or record: field, offset or order, width, type,
range.)*

## Encoding

*(Empty. Byte order, character encoding, framing or delimiting rules.)*

## Ranges and limits

*(Empty. Valid value ranges, maximum lengths, and what happens outside them —
cross-reference [`edge-cases.md`](./edge-cases.md).)*
