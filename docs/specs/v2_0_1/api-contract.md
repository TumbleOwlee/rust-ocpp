# OCPP 2.0.1 — API Contract

The area's stable public surface: exported names and signatures, error variants,
configuration fields, feature flags.

This is spec, not documentation of what happens to exist. Changing anything here
is a normative change and goes through gate 1 — it is what consumers depend on.

---

## Types

*(Empty.)*

## Functions

*(Empty.)*

## Errors

*(Empty. Every failure mode is a distinct variant — never a stringly-typed
error.)*

## Configuration

*(Empty. Field name, type, default, and the requirement ID that governs its
effect.)*

## Feature flags

*(Empty. Flag name, what it gates, whether it is default-on.)*
