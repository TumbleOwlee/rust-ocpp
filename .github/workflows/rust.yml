name: Build and Test

on:
  push:
    paths:
      - "**.rs"
    branches: [main]
  pull_request:
    paths:
      - "**.rs"
    branches: [main]
  workflow_dispatch: {}

env:
  CARGO_TERM_COLOR: always
  LLVM_PROFILE_FILE: "rust-ocpp-%p-%m.profraw"
  RUSTFLAGS: "-Cinstrument-coverage"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
      - name: download grcov for code coverage
        run: curl -L https://github.com/mozilla/grcov/releases/latest/download/grcov-x86_64-unknown-linux-gnu.tar.bz2 | tar jxf -
      - name: install llvm tools preview
        run: rustup component add llvm-tools-preview
      - name: Format check
        run: cargo fmt -- --check
      - name: Lint
        run: cargo clippy --tests --features="v1_6,v2_0_1" -- -D warnings
      - name: Build
        run: cargo build --features="v1_6,v2_0_1"
      - name: Run tests
        run: LLVM_PROFILE_FILE="your_name-%p-%m.profraw" cargo test --features="v1_6,v2_0_1"
      - name: Generate lcov.info for Code coverage
        run: ./grcov . --binary-path ./target/debug/ -s . -t lcov --branch --ignore-not-existing --ignore "/*" -o lcov.info
      - name: Codecov
        uses: codecov/codecov-action@v5.5.2
        with:
          files: ./lcov.info
          token: ${{ secrets.CODECOV_TOKEN }}

  # Coverage floor, see AGENTS.md / CONTRIBUTING.md. Separate job because
  # cargo-llvm-cov manages its own RUSTFLAGS and profile output.
  coverage:
    runs-on: ubuntu-latest
    env:
      RUSTFLAGS: ""
      LLVM_PROFILE_FILE: ""

    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: llvm-tools-preview
      - uses: Swatinem/rust-cache@v2
      - uses: taiki-e/install-action@cargo-llvm-cov
      - run: cargo llvm-cov --all-features --fail-under-lines 80
